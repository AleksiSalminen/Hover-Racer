// import React from 'react'; // Remember to re-declare Strict Mode at some point
import ReactDOM from 'react-dom/client';
import App from './App.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <App />
);
